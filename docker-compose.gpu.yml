# NVIDIA GPU override.
# Usage: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up
# Prerequisites: NVIDIA drivers and nvidia-container-toolkit

services:
  ollama:
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
